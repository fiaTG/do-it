# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Native Hüllen (Capacitor): vollständig aus capacitor.config.ts + `npx cap add`
# + `npx cap sync` regenerierbar; enthalten u. a. eine Kopie des dist-Builds.
# Sobald native Anpassungen (Icons, Permissions) dazukommen, hier entfernen.
android/
ios/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
